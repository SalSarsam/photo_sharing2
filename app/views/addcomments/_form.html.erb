<%= form_for([@addcomment.comt, @addcomment]) do |f| %>


  <% if @addcomment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@addcomment.errors.count, "error") %> prohibited this addcomment from being saved:</h2>

      <ul>
      <% @addcomment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Comment %><br />
    <%= f.text_area :content, rows: 4%>
  </div>
  <div class="actions">
    <%= f.submit %>
    
  </div>
<% end %>